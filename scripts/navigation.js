// Navigation — simplified for single-page layout (no sidebar)
(() => {
  document.addEventListener('DOMContentLoaded', () => {
    // Nothing to do — sidebar removed. Back-to-top and other UI
    // are handled by their own modules.
  });
})();
